
export interface Program {
  id: string;
  title: string;
  subtitle: string;
  features: string[];
  color: string;
  gradient: string;
}

export interface CareerItem {
  id: string;
  title: string;
  items: string[];
  icon: string;
}
